---
description: 4 周 MVP-0 范围冻结与禁止项；确保工作流优先于市场功能。
---

## 工期约束

本项目当前阶段总工期为 **4 周**，交付物为 **MVP-0**（技术 + 合规闭环），不得将交易市场、KYC、水印等大模块混入本期范围。

## MVP-0（4 周）必须交付（P0）

以 `docs/requirements/2026-06-01-mvp-voice-platform-需求规格说明.md` 的 §3.1 为准，核心是：

- 登录/手机验证
- 授权书上传与阻断训练
- 10min 素材质检与训练任务
- 单条 TTS + CSV 批量合成
- 项目/角色/音色绑定
- 敏感词拦截
- 显式标识导出（对外下载必须走合规导出）
- GPU 队列与基础配额

## MVP-0 明确禁止（4 周不做）

- 音色交易市场 UI（上架、购买、授权凭证 PDF）
- 三方实名认证 KYC
- 隐式水印/高频指纹完整方案
- 支付/提现/分账
- 完整管理后台（允许只读列表或 SQL 人工替代）
- 实时变声、实时对话低延迟链路

## 允许的运营替代（必须写清 SOP）

- 授权书审核：飞书/邮件人工审核
- 跨用户共享音色：通过 `VoiceGrant`（或等价授权表）由运营开通
- 侵权处理：邮箱受理 + 人工撤权

