# MVP-0 端到端验收记录

| 项 | 内容 |
|----|------|
| **日期** | 2026-06-10 |
| **环境** | Windows · 真引擎 9880 · `ENGINE_MOCK=false` |
| **音色** | 蛊真人-004 · `e77debe6-cad8-40a7-b754-4d68a57f4ebc` |

## 自动化脚本

| 脚本 | 覆盖 |
|------|------|
| `python scripts/smoke_e2e_guzhenren.py` | 单条合成 + 合规 wav + 20 行批量 + ZIP |
| `python scripts/smoke_e2e_batch_mixed.py` | 5 行 CSV（1 行敏感词）行级失败隔离 |

前置：`platform_start.ps1` · `engine_api_v2.ps1 -Action start` · `engine_sync_env.ps1`

## 实测结果（20 行全成功）

| 步骤 | 结果 |
|------|------|
| 单条合成 | succeeded · `data/e2e_out/single_*.wav` |
| 批量 20/20 | `succeeded_count=20` · `failed_count=0` |
| 合规 ZIP | `COMPLIANCE_README.txt` + `manifest.json` + `龙宫/0001–0020_龙宫.wav` |

报告：`data/e2e_out/e2e_report.json`

## 实测结果（mixed · 敏感词隔离）

| 步骤 | 结果 |
|------|------|
| 批量 5 行 | `succeeded_count=4` · `failed_count=1` |
| 失败行 | index 3 · `SENSITIVE_WORD` |
| ZIP | 仅含成功 4 条 wav + manifest 记录失败 |

报告：`data/e2e_out/e2e_report_mixed.json` · 脚本：`python scripts/smoke_e2e_batch_mixed.py`

## Web 手工冒烟（建议）

1. `web_dev.ps1` → `/library` 试听合成
2. `/projects` 上传 `scripts/fixtures/guzhenren_batch_20.csv` → 下载合规 ZIP
3. `/studio` 四步训练向导（可选，Mock 训练）

## 对照 SRS

| 指标 | 目标 | 本次 |
|------|------|------|
| 批量完成率 | ≥95% 行成功 | 20/20 = 100% |
| 合规导出 | 对外下载走 export | ZIP + 单条 wav ✅ |
| CSV 行数 | 建议 ≥30 行 | 20 行（可扩 `guzhenren_batch_30.csv`） |
